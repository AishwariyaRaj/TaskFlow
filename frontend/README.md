# Frontend (Vite + React)

Quick start

1. cd frontend
2. npm install
3. npm run dev

Environment
- `VITE_API_URL` - backend API base (default http://localhost:4000/api)
- `VITE_API_WS` - backend root for socket.io (default http://localhost:4000)
